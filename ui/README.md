# Storvi UI

The React/Vite frontend is developed from the repository root. Run `pnpm dev` for the API and UI together, or `pnpm --filter ui dev` for the UI only.

Production assets are generated with `pnpm build` from the root.
